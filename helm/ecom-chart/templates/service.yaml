apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecom-chart.fullname" . }}
  labels:
    {{- include "ecom-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecom-chart.selectorLabels" . | nindent 4 }}
